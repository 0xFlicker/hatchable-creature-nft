{
  "name": "@creaturenft/assets",
  "packageManager": "yarn@3.1.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "generate:notebook": "jupyter nbconvert --to notebook --execute python/generate.ipynb",
    "assets:upload": "yarn build && node ./dist/copy-assets.js"
  },
  "devDependencies": {
    "@types/node": "^17.0.8",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "minimist": "^1.2.5"
  }
}
